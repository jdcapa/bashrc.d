#!/usr/bin/env bash

LMODPATH=/usr/share/lmod/lmod/
#  MRCC set-up (quantum chemistry package)
if [ -e ${LMODPATH}/libexec/lmod ]; then
    LMOD=${LMODPATH}/libexec
    alias module=lmod
    PATH=$LMOD:$PATH
fi
